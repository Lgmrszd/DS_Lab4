version: '3.3'
services:
  web:
    build: .
#    ports:
#      - "8000:8000"
    depends_on:
      - redis
  redis:
    image: "redis:alpine"
  nginx:
    volumes:
        - ./nginx.conf:/etc/nginx/conf.d/default.conf
    ports:
      - "8040:8040"
    image: "nginx:alpine"